{
  "name": "ts-loader-declaration-output-dir-issue",
  "version": "1.0.0",
  "description": "Upgrading ts-loader to ^5.0.0 will result in the d.ts files being spit out in the root directory rather than the dist folder.",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-dev": "set NODE_ENV=development&& webpack -d",
    "build": "set NODE_ENV=production&& webpack -p",
    "watch": "webpack --watch"
  },
  "license": "ISC",
  "devDependencies": {
    "clean-webpack-plugin": "^0.1.19",
    "ts-loader": "^5.3.0",
    "typescript": "^3.1.6",
    "webpack": "^4.24.0",
    "webpack-cli": "^3.1.2"
  }
}
